#!upstart
description "Exhibitor Server"

env USER=<%= @user %>

start on startup
stop on shutdown

respawn

exec sudo -u $USER sh -c "java -jar \
    /opt/exhibitor/<%= @version %>.jar \
    <%= @opts.map { |k, v| "--#{k.to_s} #{v}" }.join(" ") %>"
